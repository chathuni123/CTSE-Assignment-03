apiVersion: v1
kind: Pod
metadata:
  name: server-pod-orderservice
  labels:
    name: server-pod-orderservice
    app: cluster-1

spec:
  containers:
    - name: server-pod-orderservice
      image: chathuni1999/cluster-1
      ports:
        - containerPort: 8080
